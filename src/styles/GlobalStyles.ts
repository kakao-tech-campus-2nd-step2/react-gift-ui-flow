import { css } from '@emotion/react';

export const GlobalStyle = css`
  * {
    box-sizing: border-box;
  }

  img {
    display: block;
  }

  ul {
    list-style: none;
    padding: 0;
    margin: 0;
  }
`;
