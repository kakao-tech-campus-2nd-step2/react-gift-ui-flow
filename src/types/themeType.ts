export type ThemeListType = {
  id: number;
  imageSrc: string;
  themeTitle: string;
};

export type ThemeHeaderType = {
  id: number;
  themeTitle: string;
  subTitle: string;
  description: string;
  color: string;
};
