import { css } from '@emotion/react';

export const countainerStyle = () =>
  css({
    height: '22rem',
    fontWeight: '800',
  });

export const titleStyle = () =>
  css({
    fontWeight: '800',
    fontSize: '2.5rem',
  });

export const buttonStyle = () =>
  css({
    fontWeight: '800',
  });
