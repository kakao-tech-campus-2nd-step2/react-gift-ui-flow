import { css } from '@emotion/css';

export default () => {
    return (
        <div
            className={css`
                height: 500px;
            `}
        >
            main
        </div>
    );
};
